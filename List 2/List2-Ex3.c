#include <stdio.h>
#include <math.h>
int main (void){
    int ano;
    printf ("insira o ano de referencia: ");
    scanf("%d", &ano);
    if (ano%100!=0 && ano %4==0){
        printf(" o ano é bissexto");
    return 0;
    }
    else(ano);
        printf("não é bissexto.");
    return 0;
return 0;
}
